interface StackType {
    name: string
}
export interface ProjectType {
    num: string
    category: string
    title: string
    description: string
    stack: StackType[]
    image: string
    live: string
    github: string
}
