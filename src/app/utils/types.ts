interface StackType {
    name: string
}
export interface ProjectType {
    num: string
    category: string
    description: string
    stack: StackType[]
    image: string
    live: string
    github: string
}
